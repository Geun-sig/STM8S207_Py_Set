# Qt GUI 컴포넌트 (위젯) 가이드

이 문서는 Python PyQt6 프로젝트에서 자주 사용되는 Qt GUI 컴포넌트(위젯)와 레이아웃 관리자의 목록입니다. 코드에서 해당 클래스 이름을 찾아 UI 요소를 파악하는 데 도움이 될 수 있습니다.

## 주요 Qt 위젯

| 클래스 이름         | 한글 설명                                      |
| :---------------- | :--------------------------------------------- |
| `QDialog`           | 독립적인 대화 상자 (팝업 창)                   |
| `QWidget`           | 모든 UI 요소의 기본이 되는 빈 위젯             |
| `QLabel`            | 텍스트나 이미지를 표시하는 라벨                |
| `QPushButton`       | 사용자가 클릭할 수 있는 버튼                   |
| `QComboBox`         | 여러 항목 중 하나를 선택하는 드롭다운 목록     |
| `QCheckBox`         | 켜고 끌 수 있는 체크박스                       |
| `QSpinBox`          | 정수 값을 선택하는 스핀 컨트롤                 |
| `QDoubleSpinBox`    | 소수점 값을 선택하는 스핀 컨트롤               |
| `QGroupBox`         | 관련된 위젯들을 그룹화하고 제목을 표시하는 상자 |
| `QTabWidget`        | 여러 페이지를 탭으로 전환하며 보여주는 위젯      |
| `QScrollArea`       | 내용이 길어지면 스크롤을 만들어주는 영역       |
| `QMessageBox`       | 정보, 경고, 질문 등을 표시하는 메시지 상자     |

## 레이아웃 관리자

| 클래스 이름         | 한글 설명                                      |
| :---------------- | :--------------------------------------------- |
| `QVBoxLayout`       | 위젯을 **세로**로 차례대로 배치                |
| `QHBoxLayout`       | 위젯을 **가로**로 차례대로 배치                |
| `QFormLayout`       | '레이블: 입력필드' 형식의 폼을 만들 때 사용    |
| `QGridLayout`       | 위젯을 표(그리드) 형태로 배치                  |

---
